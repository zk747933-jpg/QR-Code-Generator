# QR-Code-Generator
The QR Code Generator is a Python-based program that creates customized QR codes using the qrcode and Pillow libraries. It allows users to generate QR codes for text or URLs with adjustable size, color, and border, and saves the generated QR image as a .png file.
